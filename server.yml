server:
  base_url: http://localhost:8000
  storage_dir: ./store
  expiry_hours: 24

admin_users:
  - username: admin
    password: secret

tokens:
  - name: example
    token: deadbeefdeadbeefdeadbeefdeadbeef

cli:
  version: "0.1.0"
  binary_path: ./cli/devtrans
